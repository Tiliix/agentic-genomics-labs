# Open Targets is a plain GraphQL-over-HTTP API, so `requests` is sufficient.
# `gql` is listed as an optional alternative if you prefer a typed GraphQL client.
requests>=2.31
gql[requests]>=3.5            # optional: typed GraphQL client (not required by this lab)
pandas>=2.0

# Azure OpenAI agent
openai>=1.40
python-dotenv>=1.0
azure-identity>=1.17

# Azure AI Search literature-RAG (optional extension)
azure-search-documents>=11.5

# Dev tooling
ruff>=0.5
