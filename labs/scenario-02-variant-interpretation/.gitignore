# Secrets
.env

# Downloaded / generated data (regenerate with scripts/download_data.py).
# The tiny bundled data/example.vcf is intentionally kept (force-added).
data/

# Agent output — memos and cohort reports are regenerated by running the agent.
output/

# Python
__pycache__/
*.pyc
.venv/
.ruff_cache/
