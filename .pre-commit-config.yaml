# Pinned scanner for the committed hooks. See SECURITY.md for installation.
# Enable with: git config core.hooksPath .githooks
# This configuration also supports manually running pre-commit.
repos:
  - repo: https://github.com/gitleaks/gitleaks
    rev: v8.30.1
    hooks:
      - id: gitleaks
