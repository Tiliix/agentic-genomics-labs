# Pre-commit hooks for this repo. Enable once per clone:
#   pip install pre-commit && pre-commit install
# Keep pinned versions current with:  pre-commit autoupdate
repos:
  - repo: https://github.com/gitleaks/gitleaks
    rev: v8.30.1
    hooks:
      - id: gitleaks
